import React from 'react'
import QueryValidator from './QueryValidator'
import '../../baseline.css'

export default {
  component: QueryValidator,
  title: 'Components|QueryValidator'
}

export const Default = () => <QueryValidator />

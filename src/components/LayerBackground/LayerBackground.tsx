import React from 'react'
import LayerProps from '../../types/LayerProps'

const LayerBackground = ({ model }: LayerProps) => (
  <div>Here BACKGROUND properties</div>
)

export default LayerBackground
